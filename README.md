Pour compiler (dans src/) :
    javac -d ../build/ bataille/*/*.java

Pour éxécuter (dans livraison/) :
	- Sans interface :
    		java -cp build/ bataille.joue.Jouer
    	- Avec interface :
    		java -cp build/ bataille.vue.GrilleInterface
